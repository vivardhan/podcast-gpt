runtime: python311

env_variables:
  IS_APP_ENGINE: 1
  OPENAI_API_KEY: "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"
  QDRANT_API_KEY: "q2ABAu92wxB6z73dTyiuTKFNwH9w4afucpfOhxD317BeWxATE2Ww-Q"

entrypoint: gunicorn main:app --threads 4 --workers 1 -c /config/gunicorn.py

handlers:
  # This configures Google App Engine to serve the files in the app's static
  # directory.
- url: /static
  static_dir: static

  # This handler routes all requests not caught above to your main app. It is
  # required when static routes are defined, but can be omitted (along with
  # the entire handlers section) when there are no static files defined.
- url: /.*
  script: auto